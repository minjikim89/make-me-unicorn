# Session Close Prompt

Update project state based on today's work.

1. Update completed/incomplete items in `current_sprint.md`.
2. If new decisions were made, create ADR drafts in `docs/adr/`.
3. Apply minimal updates to changed `docs/core/*` or `docs/ops/*` files.
4. Update `Handoff Note` so the next session can continue immediately.

Output format:

- Completed:
- Incomplete:
- New ADR:
- Updated docs:
- Next session first task:
