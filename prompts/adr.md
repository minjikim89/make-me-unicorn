# ADR Generation Prompt

Create an ADR for the decision we just made.

Rules:

1. Fill `Context`, `Options`, `Decision`, `Why`, `Consequences`, `Revisit Trigger`.
2. Include rejected alternatives.
3. Add one-line operational impact in relevant domains (`auth`, `billing`, `compliance`, `growth`).
4. Suggest filename format: `docs/adr/NNN_short_title.md`.

Output:

- Suggested filename:
- ADR markdown:
